# Dependencies that SAB needs and the pinned serving images do not contain.
# pip installs them with --no-deps. No entry here can add or upgrade torch,
# tensorrt, onnxruntime, numpy, or any other part of the serving stack.
# The list is curated per base image. The import check in each Dockerfile
# finds drift at build time.
fire==0.7.0
faster-coco-eval==1.6.7
tqdm==4.67.1
